The functions used in each process (generate, sign, verify) in the DS2 protocol are described in src/bin/gen.rs, sign.rs, verify.rs.
To actually run the entire protocol, run src/bin/ds2.rs with the command cargo run --bin ds2.
